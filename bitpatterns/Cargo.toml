[package]
name = "bitpatterns"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bitpatterns-proc-macro = { path = "../bitpatterns-proc-macro", version = "=0.1.0" }

[features]
display = []